import { ActivityIndicator } from 'react-native'; 
import { Container } from './styles';

export default LoadPage = () => {
    return (
        <Container>
            <ActivityIndicator />
        </Container>
    );
};